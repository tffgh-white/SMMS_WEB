# 前后端服务部署指南

## 🌐 局域网访问配置

已配置前端Vite开发服务器和后端Express服务器支持局域网访问。

---

## 🎯 前端服务部署 (Vue项目)

### 🚀 快速启动

#### Windows用户
```bash
# 方法1：直接运行npm命令
npm run dev

# 方法2：使用提供的批处理脚本
start-server.bat
```

#### Linux/Mac用户
```bash
# 方法1：直接运行npm命令
npm run dev

# 方法2：使用提供的shell脚本
chmod +x start-server.sh
./start-server.sh
```

### 📡 前端访问地址

- **本地访问**: http://localhost:5173
- **局域网访问**: http://[你的IP地址]:5173

---

## 🔧 后端服务部署 (Node.js API服务器)

### 🚀 快速启动

#### Windows用户
```bash
# 方法1：直接运行
cd server
node server.js

# 方法2：使用提供的批处理脚本
start-backend.bat
```

#### Linux/Mac用户
```bash
# 方法1：直接运行
cd server
node server.js

# 方法2：使用提供的shell脚本
chmod +x start-backend.sh
./start-backend.sh
```

### 📡 后端访问地址

- **本地访问**: http://localhost:3001
- **局域网访问**: http://[你的IP地址]:3001

### 🔧 后端配置说明

Express服务器已配置为：
- `host: '0.0.0.0'` - 监听所有网络接口
- `port: 3001` - 固定端口3001
- CORS支持所有来源访问

---

## 🌐 完整局域网部署

### 📋 启动步骤

1. **启动后端服务器**
   ```bash
   # Windows
   start-backend.bat
   
   # Linux/Mac
   chmod +x start-backend.sh && ./start-backend.sh
   ```

2. **启动前端服务器**
   ```bash
   # Windows
   start-server.bat
   
   # Linux/Mac
   chmod +x start-server.sh && ./start-server.sh
   ```

3. **获取本机IP地址**
   ```bash
   # Windows
   ipconfig
   
   # Linux/Mac
   ifconfig
   ```

4. **局域网设备访问**
   - 前端页面: http://[你的IP地址]:5173
   - API接口: http://[你的IP地址]:3001

### 📱 移动设备测试

1. 确保手机/平板与电脑在同一WiFi网络
2. 在手机浏览器中输入 `http://[电脑IP]:5173`
3. 应用会自动连接到同一IP的后端服务器

### 🔍 服务状态检查

#### 前端服务检查
- 浏览器访问 http://localhost:5173 确认前端正常运行
- 检查控制台是否显示 "Local: http://localhost:5173" 和 "Network: http://[你的IP]:5173"

#### 后端服务检查
- 浏览器访问 http://localhost:3001/api/users 确认API正常
- 检查终端是否显示 "服务器运行在 http://localhost:3001"

---

## 🛠️ 故障排除

### 🔥 防火墙问题

#### Windows
1. 在防火墙设置中允许Node.js通过防火墙
2. 或者临时关闭Windows防火墙测试

#### Linux
```bash
# 开放5173和3001端口
sudo ufw allow 5173
sudo ufw allow 3001
```

#### Mac
在系统偏好设置 > 安全性与隐私 > 防火墙中允许入站连接

### 🌐 网络连接问题

1. **确认设备在同一局域网**
   - 所有设备连接到相同的WiFi网络
   - 或者通过网线连接到同一路由器

2. **检查IP地址**
   ```bash
   # Windows
   ipconfig | findstr "IPv4"
   
   # Linux/Mac
   ifconfig | grep "inet "
   ```

3. **端口占用检查**
   ```bash
   # Windows
   netstat -ano | findstr :5173
   netstat -ano | findstr :3001
   
   # Linux/Mac
   lsof -i :5173
   lsof -i :3001
   ```

4. **服务连通性测试**
   ```bash
   # 测试端口是否开放
   telnet [IP地址] 5173
   telnet [IP地址] 3001
   ```

### 📱 移动端无法访问

1. **检查网络连接**
   - 确保手机和电脑在同一WiFi
   - 尝试访问其他局域网服务验证网络

2. **检查CORS配置**
   - 后端已配置支持所有来源访问
   - 如仍有问题，检查浏览器控制台错误信息

---

## 🎯 生产环境部署

### 前端构建
```bash
npm run build
# 构建文件在 dist 目录
```

### 后端生产配置
1. 使用PM2进程管理器
2. 配置HTTPS证书
3. 使用数据库替代内存存储
4. 配置环境变量

---

## 📋 配置文件位置

### 前端配置
- Vite配置: `vite.config.ts`
- 前端启动脚本: `start-server.bat` / `start-server.sh`
- 前端端口: 5173

### 后端配置
- Express服务器: `server/server.js`
- 后端启动脚本: `start-backend.bat` / `start-backend.sh`
- 后端端口: 3001

---

## 🔗 相关文档

- [Vite服务器配置](https://vite.dev/config/server-options/)
- [Express服务器部署](https://expressjs.com/en/guide/deployment.html)
- [Vue项目部署](https://vuejs.org/guide/quick-start.html#building-for-production)

---

**注意事项**:
1. 开发服务器仅用于测试和开发
2. 生产环境请使用专业Web服务器和数据库
3. 确保网络安全，避免暴露敏感信息
4. 定期更新依赖包以修复安全漏洞